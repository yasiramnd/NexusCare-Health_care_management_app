Run admin portal:

1. npm install
2. npm start

Environment variables:

1. This portal uses Create React App, so backend URL must be set with REACT_APP_API_URL.
2. Example:

Note:

1. VITE_API_URL is not read by Create React App builds.
